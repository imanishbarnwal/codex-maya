OPENAI_API_KEY=sk-proj-your-key-here
OPENAI_MODEL=gpt-4.1-mini

# Telegram — let Arjun send DMs via chat ("message Prachi: hi")
# Get a token from @BotFather. Either name works (lowercase for convenience):
telegram_bot_key=123456:ABC-your-bot-token
# Optional: pin chat IDs so lookups don't rely on getUpdates history
# Format: name=chat_id pairs separated by commas
# TELEGRAM_CONTACTS=prachi=1490453609,rahul=987654321
